
## This is a file to set up the dependencies without installing them to the system. Useful if you are working on a larger shared system (HPC, cluster, etc....)

pip install -r requirements.txt -t src/

